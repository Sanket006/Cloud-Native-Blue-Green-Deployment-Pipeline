apiVersion: v1
kind: Service
metadata:
  name: bg-demo-service
  labels:
    app: demo-app
spec:
  type: NodePort
  selector:
    app: demo-app
    # The version label dictates which deployment gets traffic!
    version: blue
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080
